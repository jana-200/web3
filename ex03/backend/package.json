{
  "name": "backend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "dev": "nodemon --exec npm run start",
    "build": "npm install && npm run prisma:generate ",
    "prisma:generate": "prisma generate --no-engine",
    "//prisma:start_db": "We need a specific version of prisma to fix a bug on windows",
    "prisma:start_db": "npx prisma dev @0.0.0-dev.202509301625"
  },
  "dependencies": {
    "@prisma/client": "^6.18.0",
    "cookie-parser": "~1.4.4",
    "cors": "^2.8.5",
    "debug": "~2.6.9",
    "dotenv": "^17.2.3",
    "express": "~4.16.1",
    "morgan": "~1.9.1"
  },
  "devDependencies": {
    "prisma": "^6.18.0"
  }
}
